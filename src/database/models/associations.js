'use strict';

/**
 * Mendefinisikan semua asosiasi antar model Sequelize.
 * Dipanggil sekali saat aplikasi start (di app.js).
 *
 * Tambahkan asosiasi model baru di sini.
 */

// const Users = require('./usersModel.js');
// Contoh:
// Users.belongsTo(SomeModel, { foreignKey: 'someId', as: 'someAlias' });

function setupAssociations() {
  // Tambahkan asosiasi di sini
}

module.exports = setupAssociations;
